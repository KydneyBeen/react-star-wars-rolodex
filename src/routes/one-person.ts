import * as https from 'https';
import * as dotenv from 'dotenv';

dotenv.config({
  path: __dirname + '/../.env',
});

const onePerson = (req: any, res: any, next: VoidFunction): void => {};

export default onePerson;
