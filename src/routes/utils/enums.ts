export enum Resource {
  people = 'people',
  films = 'films',
  species = 'species',
  planets = 'planets',
}

export enum Gender {
  male = 'male',
  female = 'female',
  unknown = 'unknown',
  na = 'n/a',
}
