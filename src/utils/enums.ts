export enum Gender {
  male = 'male',
  female = 'female',
  unknown = 'unknown',
  na = 'n/a',
}

export enum Resource {
  people = 'people',
  films = 'films',
  species = 'species',
  planets = 'planets',
}
